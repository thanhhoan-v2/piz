// Placeholder service after removing deepseek dependency

export async function getCodeReview(code: string, language?: string | null) {
	// Return a message indicating the feature is no longer available
	return "Code review feature is currently unavailable. The deepseek library has been removed."
}
