enum AttachmentFormat {
  AUDIO
  IMAGE
  VIDEO
}

enum AttachmentType {
  POST
  COMMENT
}

enum ContentType {
  POST
  COMMENT
}

enum FollowStatus {
  PENDING
  ACCEPTED
  REJECTED
}

enum NotificationType {
  FOLLOW
  FOLLOW_SUGGEST
  TAG
  COMMENT
  REACT
}

enum PostActionType {
  SAVE
  IGNORE
}

enum PostVisibility {
  PUBLIC
  FRIENDS_ONLY
  JUST_ME
}

enum PrivacyMode {
  PUBLIC
  PRIVATE
}

enum ReactionType {
  LOVE
  HATE
}

enum ReportType {
  SPAM
  HARASSMENT
  INAPPROPRIATE
}
